name: Deploy Fly API

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-fly-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    uses: ./.github/workflows/deploy-api.yml
    secrets: inherit
