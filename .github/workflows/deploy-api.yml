name: Deploy API

on:
  workflow_dispatch:
  workflow_call:

permissions:
  contents: read

concurrency:
  group: deploy-api-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: "22"
  FLY_APP_NAME: infamous-freight-api
  FLY_APP_URL: https://infamous-freight-api.fly.dev
  FLY_API_URL: https://api.infamousfreight.com

jobs:
  deploy:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@8912a9102ac27614460f54aedde9e1e7f9aec20d # v4.1.0

      - name: Setup Node.js
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client
        run: pnpm -C apps/api run prisma:generate

      - name: Build API
        run: pnpm -C apps/api run build

      - name: Setup flyctl
        uses: superfly/flyctl-actions/setup-flyctl@ed8efb33836e8b2096c7fd3ba1c8afe303ebbff1 # master (2026-04-08)

      - name: Deploy to Fly.io
        run: |
          set -euo pipefail
          max_attempts=3
          attempt=1
          until [ "$attempt" -gt "$max_attempts" ]; do
            echo "Fly deploy attempt $attempt/$max_attempts"
            if flyctl deploy --remote-only --config fly.toml --app "$FLY_APP_NAME" --wait-timeout 10m --strategy rolling; then
              break
            fi
            if [ "$attempt" -eq "$max_attempts" ]; then
              echo "::error::Fly deploy failed after $max_attempts attempts."
              exit 1
            fi
            sleep $((attempt * 30))
            attempt=$((attempt + 1))
          done
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - name: Verify deployment health
        run: scripts/fly-post-deploy-check.sh "$FLY_APP_NAME" "$FLY_APP_URL" "$FLY_API_URL"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - name: Write deploy summary
        if: always()
        run: |
          {
            echo "## Deploy API"
            echo ""
            echo "- App: \`$FLY_APP_NAME\`"
            echo "- Fly URL: $FLY_APP_URL"
            echo "- API URL: $FLY_API_URL"
            echo "- SHA: ${{ github.sha }}"
          } >> "$GITHUB_STEP_SUMMARY"
